// payload.config.ts (NOOP)
// The Payload integration was removed during the repository reset.
// This file exists only as a safe placeholder to prevent accidental imports
// from pulling in runtime Payload code or referencing deleted collections.

const noopConfig: Record<string, any> = {
  serverURL: null,
  secret: null,
  db: null,
  admin: {},
  plugins: [],
  collections: [],
  globals: [],
};

export default noopConfig;
