// posts collection placeholder (Payload runtime removed)
// This file intentionally contains no imports from `payload` or runtime code.
// It remains as a scaffold for future re-integration if desired.

const Posts = {};

export default Posts;
