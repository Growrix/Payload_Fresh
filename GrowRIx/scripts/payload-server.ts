// scripts/payload-server.ts (NOOP)
// Disabled during repository reset to prevent accidental Payload initialization.
// No imports from `payload` are present here.
console.log('payload-server script disabled during project reset.');
